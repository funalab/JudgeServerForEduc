# 4. <img src="./images/docker.png" width="24" style="vertical-align:middle;"> Install Docker

Example (Mac):

```bash
cd ~/Applications
sudo port selfupdate
sudo hdiutil attach ~/Downloads/Docker.dmg
sudo /Volumes/Docker/Docker.app/Contents/MacOS/install
```

Skip this step if Docker is already installed.
